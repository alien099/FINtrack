meta:
  environment: python
  toolchain:
    name: pip
    version: 3.9

build:
  requirementsPath: requirements.txt

run:
  scriptName: python manage.py migrate && python manage.py collectstatic --noinput && gunicorn config.wsgi:application
  persistenceMount: /data
  containerPort: 8000